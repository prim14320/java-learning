## 9月1-6号学习笔记

#### 1.`Java` 中 `=` 赋值对数组引用没有大小限制——因为改的是**指向**，不是数组本身。

但要注意：

```java
Student student[] = new Student[3];
student = new Student[5];  // ✅ 可以，引用重新指向
student = new Student[1];  // ✅ 也可以，引用重新指向
```

我的理解：原来的数组变量在栈里面存有一个地址，堆里存放着数组元素，这个地址指向这个堆，当把原来的数组重新赋值时改变的是这个地址，也就是引用

#### 2.`NullPointerException`（空指针异常）

抛出的完整错误信息是：

```java
Exception in thread "main" java.lang.NullPointerException: Cannot invoke "test4.Student.getId()" because "student[i]" is null
	at test4.Test.main(Test.java:27)
```

在删除数组元素时，简单地将删除位置置空，忽略了在循环时对空的方法调用无法实现导致报错；

#### 3.创建字符串对象两种方式的区别

- 通过构造方法创建的每`new`一次都会申请一个内存空间，地址不一样
- 直接赋值创建的会在串池里面寻找一样的字符串，有一样的便不会改变地址值

#### 4.`==`的作用

- 比较基本数据类型：比较的是具体的值
- 比较引用数据类型：比较的是对象的地址值

#### 5.`this`关键字的作用

可以区别成员变量和局部变量

#### 6.键盘录入的两套体系

**1.第一套**

1. `nextInt`( );接受整数
2. ``nextDouble()` ;接受小数
3. `next()`;接受字符串

**注意事项:遇到空格，制表符，回车停止接收，这些符号后面的数据就不会接受了**

**2**.**第二套**

1. `newline()`；接受字符串

#### 7.`replace,substring,StringBuilder,StringJoiner`的用法

1. ##### `replace`

   `String replace(旧值,新值);`

   ```java
   public class Test7 {
       public static void main(String[] args) {
           Scanner sc = new Scanner(System.in);
           System.out.println("请输入一个字符串");
           String str = sc.next();
           replacemore(str);
   //        str = str.replace("TMD", "***");
   //        System.out.println(str);
       }
   
       public static void replacemore(String str) {
           String[] arr = {"TMD", "NMB", "CNM"};
           for (int i = 0; i < arr.length; i++) {
               str = str.replace(arr[i], "***");
           }
           System.out.println(str);
       }
   }
   ```

   **注意：只有返回值才是替换后的结果**；

2. ##### `substring`

   两种方式截取字符串传入一个索引就是从该索引开始截取到字符末尾，传入两个就是从该字符串的第一个索引开始截取到第二个索引前一个字符。

   注意事项:

   1. **索引从 0 开始**，左闭右开 `[beginIndex, endIndex)`
   2. 如果传入的索引超出字符串长度，会抛出 `StringIndexOutOfBoundsException`
   3. 调用 `substring` 不改变原字符串（String 是不可变的），而是返回新字符串

   ```java
   /以字符串形式从键盘接收一个手机号，将中间四位屏蔽，输出：138****45678
   //功能：键盘录入，用substring截取字符，在进行拼接以达到屏蔽效果；
   public class Test6 {
       public static void main(String[] args) {
           Scanner sc = new Scanner(System.in);
           System.out.println("请输入一个手机号");
           String phone = sc.next();
           String start = phone.substring(0, 3);
           String end = phone.substring(7);
           String result = start+ "****"+ end;
           System.out.println(result);
       }
   }
   ```

3. ##### `StringBuilder`

   `StringBuilder` 是一个**可变的字符串类**，用于高效地拼接、修改字符串。

   优点：Java 中 `String` 是**不可变的**（immutable），每次对 `String` 做拼接或修改，都会在内存中**创建新的字符串对象**，效率很低。

   **常见构造方法**

   ```java
   new StringBuilder()           // 空构造，默认容量16
   new StringBuilder("Hello")    // 传入初始字符串
   new StringBuilder(32)         // 指定初始容量
   ```

    **常用方法**

   | 方法                       | 说明           | 示例                     |
   | :------------------------- | :------------- | :----------------------- |
   | `append(xxx)`              | 追加内容       | `sb.append("abc")`       |
   | `reverse()`                | 反转字符串     | `sb.reverse()`           |
   | `insert(offset, xxx)`      | 在指定位置插入 | `sb.insert(2, "X")`      |
   | `delete(start, end)`       | 删除指定范围   | `sb.delete(1, 3)`        |
   | `replace(start, end, str)` | 替换指定范围   | `sb.replace(1, 3, "XY")` |
   | `toString()`               | 转换为 String  | `sb.toString()`          |
   | `length()`                 | 返回长度       | `sb.length()`            |

   **示例代码**

   ```java
   //判断是否为对称字符串，用StingBuilder
   //思路：键盘录入一个字符串，用一个变量接受，再创建一个StringBuilder对象添加该元素，反转后转换为字符串与原字符串进行比较
   public class Test10 {
       public static void main(String[] args) {
           Scanner sc = new Scanner(System.in);
           System.out.println("请输入一个字符串");
           String str = sc.next();
   //链式编程法；
           String str2 = new StringBuilder().append(str).reverse().toString();
   //        StringBuilder sb = new StringBuilder(str);
   //        sb.reverse();
   //        String str2 = sb.toString();
           if(str.equals(str2)){
               System.out.println("是对称字符串");
           }else{
               System.out.println("不是对称字符串");
   
           }
       }
   }
   ```

4. ##### `StringJoiner`

   `StringJoiner` 是 Java 8 引入的类，用于**高效地拼接字符串，并在元素之间自动添加分隔符**。它就像是给 `StringBuilder` 加上了"自动加分隔符"的能力。

   **构造方法**

   ```java
   // 方式一：只指定分隔符
   new StringJoiner("分隔符")
   // 方式二：指定分隔符 + 前缀 + 后缀
   new StringJoiner("分隔符", "前缀", "后缀")
   ```

​	**常用方法**

| 方法                          | 说明                           |
| :---------------------------- | :----------------------------- |
| `add(CharSequence)`           | 添加一个元素                   |
| `merge(StringJoiner)`         | 合并另一个 StringJoiner 的内容 |
| `toString()`                  | 输出最终拼接结果               |
| `length()`                    | 返回当前长度                   |
| `setEmptyValue(CharSequence)` | 当没有元素时返回的默认值       |

​     **基础用法示例**

```java
StringJoiner sj = new StringJoiner(", ");
sj.add("苹果");
sj.add("香蕉");
sj.add("橘子");
System.out.println(sj);  // 输出: 苹果, 香蕉, 橘子
```

​     **带前缀后缀的用法**

```java
StringJoiner sj = new StringJoiner(", ", "[", "]");
sj.add("A");
sj.add("B");
sj.add("C");
System.out.println(sj);  // 输出: [A, B, C]
```

#### 8.初识`ArryList`的简单用法

1. `ArryList`概述

   - 什么是集合

     提供一种存储空间可变的存储模式，存储的数据容量可以发生改变

   - `ArryList`集合的特点

     长度可以发生变化，只能存储引用类型数据

   - 泛类的使用

     用于约束集合中存储元素的数据类型

2. 代码示例

   ```java
   public static void main(String[] args) {
           //创建集合
           ArrayList<String> al = new ArrayList<>();
           //添加元素
           al.add("234");
           al.add("505934");
           al.add("123");
           al.add("456");
           System.out.println(al);
           //各种方法调用
           System.out.println(al.size());//返回集合中的元素个数
   //        System.out.println(al.remove("234"));//删除指定元素返回是否删除成功
           System.out.println(al.remove(0));//删除指定索引的元素
           System.out.println(al.size());//返回集合中的元素个数
           System.out.println(al.get(2));//返回指定索引的元素
           System.out.println(al.set(2, "3473297492"));//设置指定索引的元素
           System.out.println(al);
   
   
       }
   ```

   

   

   ​	

